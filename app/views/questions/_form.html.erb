<%= simple_form_for(@question, role: "form") do |f| %>

  <%= f.input :title, label: "问题标题" %>
  <%= f.input :description, label: "问题描述", as: :text, input_html: { id: "txtNewQuestion" } %>
  <%= f.button :submit, "保存问题" %>

<% end %>
